TranslationConfig = {
    --- Language (Lua-side only)
    ---
    --- Controls the language for server and client Lua messages.
    ---
    --- This does NOT affect the web interface (NUI).
    --- To change the web interface language, edit web/src/i18n/index.ts.
    ---
    --- Must match a file name in the translations/ folder (without .lua).
    ---
    --- Available: 'en', 'fr'
    ---
    --- Default: 'en'
    language = 'en',
}
